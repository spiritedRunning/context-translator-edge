{
  "name": "context-translator",
  "version": "0.1.5",
  "private": true,
  "type": "module",
  "description": "AI-powered hover/selection translation browser extension for Microsoft Edge, with a per-page context session.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.0",
    "@types/chrome": "^0.2.0",
    "typescript": "^6.0.3",
    "vite": "^8.1.0"
  },
  "dependencies": {
    "dompurify": "^3.4.11"
  }
}
